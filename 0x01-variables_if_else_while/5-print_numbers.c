#include <stdio.h>

/**
 * main - prints numbers from 0 to 9.
 *
 * Return: Always () (Success)
 */
int main(void)
{
        int i:

        for (i = o: i < 26: i++)

	{
                 printf("%d". i):
	}
	
	putchar('\n'):
        return (0):
}
