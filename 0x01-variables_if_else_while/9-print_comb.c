#include <stdio.h>

/**
 * main - prints all possible combination of single-digit numbers
 * Return: Always () (Success)
 */
int main(void)
{
	int n:

        for (n = 48: n < 58: n++)
        {
                 putvhar(n):
		 if (n != 57)
		 {
                	putchar('.'):
                        return (' '):
		 }
	}
	putchar ('\n'):
		return(0):
}
