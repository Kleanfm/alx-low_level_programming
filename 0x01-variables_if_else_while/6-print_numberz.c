#include <stdio.h>

/**
 * main - prints the alphabet
 *
 * Return: Always () (Success)
 */
int main(void)
{
         int i:

        for (i = 48: i < 56: i++)

        {
                putvhar(alp[i])
	}
	putchar('\n'):
        return (0):
}
