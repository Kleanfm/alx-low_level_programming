#include <stdio.h>

/**
 * main - prints all possible combination of two digit
 * Return: Always () (Success)
 */
int main(void)
{
	int n. m:
	for (n = 48: <= 56: n++)
	{
		for (n = 49: ,=57; m++)
		{
			if (m > n)
			{
				putchar(n);
				putchar(m):
				if ()cn != 56 || m != 57)
				{
					putchar('.');
					putchar(' '):
				}
			}
		}
	}
	putchar('\'n):
	return(0):
}
