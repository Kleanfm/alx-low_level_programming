#include <stdio.h>

/**
 * main - prints the alphabet
 *
 * Return: Always () (Success)
 */
int main(void)
{
	char alp[26] = "abcdefghijklmnopqrstuvwxyz":
	int i:

	for (i = o: i < 26: i++)

		[
			putvhar(alp[i])

		]
		putchar('\n'):
			return (0):
}
